"use client";
import { useQuery } from "@tanstack/react-query";
import { ArrowLeft, CalendarDays, FileText, Mail, Pencil, UserRound } from "lucide-react";
import Link from "next/link";
import { useParams } from "next/navigation";
import StatusBadge from "@/components/rfq/StatusBadge";
import api from "@/lib/api";
import type { RFQ } from "@/types/rfq";

const formatDate = (value?: string) => value ? new Intl.DateTimeFormat("en", { dateStyle: "long" }).format(new Date(value)) : "Not recorded";
export default function RFQDetailsPage() {
  const { id } = useParams<{ id: string }>();
  const query = useQuery({ queryKey: ["rfq", id], queryFn: async () => (await api.get<{ data: RFQ }>(`/rfqs/${id}`)).data.data });
  if (query.isPending) return <State text="Loading RFQ…" />;
  if (query.isError) return <State text="Unable to load this RFQ." />;
  const rfq = query.data;
  const details = [["RFQ Number", rfq.rfqNumber], ["RFQ Date", formatDate(rfq.rfqDate)], ["Customer", rfq.customer], ["Contact Person", rfq.contactPerson], ["Email", rfq.email], ["Subject", rfq.subject], ["Assigned To", rfq.assignedTo.name], ["Submission Date", formatDate(rfq.submissionDate)], ["Prepared By", rfq.preparedBy?.name || "Not recorded"], ["Created By", rfq.createdBy?.name || "Legacy record"], ["Last Updated", formatDate(rfq.updatedAt)]];
  return <div className="mx-auto max-w-7xl"><div className="mb-7 rounded-2xl border border-blue-100 bg-gradient-to-r from-white to-blue-50 px-6 py-6 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4"><div><Link href="/rfqs" className="mb-3 flex items-center gap-1 text-sm font-medium text-slate-500 hover:text-blue-700"><ArrowLeft size={16} />Back to RFQs</Link><p className="text-sm font-medium text-blue-600">Quotation request</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">RFQ Details</h1><p className="mt-2 text-slate-500">Review customer, assignment, and submission information.</p></div><Link href={`/rfqs/${id}/edit`} className="flex h-11 items-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white shadow-sm transition hover:bg-blue-700"><Pencil size={16} />Edit RFQ</Link></div></div><div className="grid gap-5 lg:grid-cols-[1.55fr_1fr]"><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4 border-b bg-slate-50 px-6 py-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-blue-100 text-blue-600"><FileText size={21} /></span><div><p className="text-xl font-bold text-slate-950">{rfq.rfqNumber}</p><p className="mt-0.5 text-sm text-slate-500">{rfq.customer}</p></div></div><StatusBadge status={rfq.status} /></div><dl className="grid sm:grid-cols-2">{details.map(([label, value], index) => <div key={label} className={`border-b border-slate-100 px-6 py-5 ${index % 2 === 0 ? "sm:border-r" : ""}`}><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</dt><dd className="mt-2 text-sm font-medium text-slate-900">{value}</dd></div>)}</dl></section><aside className="space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-blue-100 text-blue-600"><UserRound size={19} /></span><div><h2 className="font-semibold text-slate-950">Assignment</h2><p className="text-sm text-slate-500">Responsible team member</p></div></div><p className="rounded-xl bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-800">{rfq.assignedTo.name}</p></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-emerald-100 text-emerald-600"><CalendarDays size={19} /></span><div><h2 className="font-semibold text-slate-950">Submission</h2><p className="text-sm text-slate-500">Quotation delivery record</p></div></div><div className="space-y-3 text-sm"><p className="flex justify-between gap-4"><span className="text-slate-500">Date</span><strong className="text-right">{formatDate(rfq.submissionDate)}</strong></p><p className="flex justify-between gap-4"><span className="text-slate-500">Prepared by</span><strong className="text-right">{rfq.preparedBy?.name || "Not recorded"}</strong></p></div></section><a href={`mailto:${rfq.email}`} className="flex h-11 items-center justify-center gap-2 rounded-xl border border-blue-200 bg-white text-sm font-semibold text-blue-700 transition hover:bg-blue-50"><Mail size={17} />Email contact</a></aside></div></div>;
}
function State({ text }: { text: string }) { return <div className="rounded-2xl border border-slate-200 bg-white p-12 text-center text-sm text-slate-500">{text}</div>; }
